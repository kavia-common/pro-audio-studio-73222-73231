import React from 'react';

/**
 * PUBLIC_INTERFACE
 * ModalHost is a placeholder to render modals. Hook into global state to show dialogs.
 */
export default function ModalHost() {
  // This can be wired to a modal state for settings/export dialogs
  return null;
}
